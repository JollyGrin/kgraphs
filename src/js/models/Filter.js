export default class Filter {
    constructor(country, grade, min, max, cur) {
        this.country = country;
        this.grade = grade;
        this.min = min;
        this.max = max;
        this.cur = cur;
    }
};